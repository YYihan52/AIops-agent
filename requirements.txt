claude-agent-sdk>=0.1.0
pydantic>=2.0
# memory / RAG (incident ticket vector store + local embedding)
pymilvus>=2.4
sentence-transformers>=2.2
modelscope>=1.9  # China-friendly model download for the BGE embedding
